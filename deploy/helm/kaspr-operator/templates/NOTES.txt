{{ $.Chart.Name }} has been installed.

  {{- if .Values.operator }}
  Check its status by running:
  kubectl --namespace {{ template "kaspr.operator.namespace" . }} get pods -l "app.kubernetes.io/name={{ include "kaspr.operator.name" . }}"
  {{- end }}

Visit https://github.com/kaspr-operator/ for instructions on how to create & configure Kaspr custom resources.